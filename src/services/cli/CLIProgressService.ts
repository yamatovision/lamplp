/*
 * このファイルは使用されていないため、無効化されています。
 * CLILauncherService が ClaudeCodeLauncherService に置き換えられています。
 * 必要になったときに再度有効化してください。
 */
 
/*
import * as vscode from 'vscode';
import * as fs from 'fs';
import * as path from 'path';
import { Logger } from '../../utils/logger';
import { AppGeniusEventBus, AppGeniusEventType } from '../AppGeniusEventBus';
import { AppGeniusStateManager, ImplementationItem } from '../AppGeniusStateManager';

// CLIExecutionStatus を自前で定義
enum CLIExecutionStatus {
  IDLE = 'idle',
  RUNNING = 'running',
  COMPLETED = 'completed',
  FAILED = 'failed',
  PAUSED = 'paused'
}

export class CLIProgressService {
  // モックメソッド
  public static getInstance(): any {
    return { 
      dispose: () => {} 
    };
  }
}
*/